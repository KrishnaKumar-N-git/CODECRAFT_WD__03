{
  "name": "grocmart-grocery-app",
  "version": "1.0.0",
  "description": "Full-stack single store grocery web application built with React, Node.js, Express & MongoDB",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install",
    "seed": "node server/seed/seed.js",
    "postinstall": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["grocery", "ecommerce", "react", "express", "mongodb", "grocmart"],
  "author": "GrocMart Team",
  "license": "ISC"
}
